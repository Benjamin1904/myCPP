一、程序结构
1.单文件

#include <iostream>
#include <头文件>

using namespace std;

#define 常量声明
const 常量声明

struct定义

class{}定义

函数声明

int main()
{
    statement;
    return 0;
}

函数定义


2.多文件(main.cpp/head.h/fun.cpp)
head.h
    #define 常量定义
    struct定义
    class   classname
    {
        public:
            公共函数声明
        private:
            私有属性
    }

fun.cpp
    #include <iostream>
    #include <头文件>

    函数定义

main.cpp

#include <iostream>
#include <头文件>
using namespace  std;

const 全局常量

int main()
{
    statement;
    return 0;
}

二、数据类型
    见思维导图

    数组：
    声明：typeName arrayName[arraySize];
        //通过从0开始编号的索引标识访问数组中的各个元素
    赋值：
        在数组声明时可初始化：int array[arraySize] = {0,1,...}；
        array[0]= 3;
    字符串：
        C-风格字符串


三、语句
    循环
        for循环
        for(init-expression; test-expression; update-expression)
        {
            statement(s)
        }

        while循环
        init-expression;
        while(test-expression)
        {
            statement(s)
            update-expression;
        }

        do-while循环
        do
            body
        while(test-expression)

        基于范围的for循环
        double prices[5] = {};
        for (dobule x : prices)
            body
        

        cin读取到EOF时停止读取输入，使用cin.fail()判断是否读取到EOF，若继续读取输入，需使用cin.clear().也可使用while(cin)

    分支
        if else语句
        if (test-expression)
		{
			statement1
		}
		else if (test-expression)
		{
			statement2
		}
		else
		{
			statement3
		}
    
        ?:
            expression1 ? expression2 : expression3
        
        switch
        switch (integer-expression)
        {
            case label1:statement(s)
            case lable2:statement(s)
            ...
            default  ：statement(s)
        }

        对于枚举中的量，本质上是整数

函数
一
    函数声明
        使编译器正确处理函数返回值，检查参数数目与类型是否正确。
        typeName functionName(parameterList);
    函数定义
        typeName functionName(parameterList)
        {
            statement(s)
            return value;
        }
    函数调用

    1.