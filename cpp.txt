一、程序结构
1.单文件

#include <iostream>
#include <头文件>

using namespace std;

#define 常量声明
const 常量声明

struct定义

class{}定义

函数声明

int main()
{
    statement;
    return 0;
}

函数定义


2.多文件(main.cpp/head.h/fun.cpp)
head.h
    #define 常量定义
    struct定义
    class   classname
    {
        public:
            公共函数声明
        private:
            私有属性
    }

fun.cpp
    #include <iostream>
    #include <头文件>

    函数定义

main.cpp

#include <iostream>
#include <头文件>
using namespace  std;

const 全局常量

int main()
{
    statement;
    return 0;
}

二、数据类型
    见思维导图

    数组：
    声明：typeName arrayName[arraySize];
        //通过从0开始编号的索引标识访问数组中的各个元素
    赋值：
        在数组声明时可初始化：int array[arraySize] = {0,1,...}；
        array[0]= 3;
    字符串：
        C-风格字符串


三、语句
    循环
        for循环
        for(init-expression; test-expression; update-expression)
        {
            statement(s)
        }

        while循环
        init-expression;
        while(test-expression)
        {
            statement(s)
            update-expression;
        }

        do-while循环
        do
            body
        while(test-expression)

        基于范围的for循环
        double prices[5] = {};
        for (dobule x : prices)
            body
        
    